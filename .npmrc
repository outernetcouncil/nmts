store-dir=.pnpm-store

# Wait at least 7 days before resolving newly published package versions.
# Newly published (or freshly compromised) versions are the primary vector for
# npm supply-chain attacks; a release-age floor gives time for malicious
# releases to be reported and unpublished before we pull them in.
min-release-age = 7
