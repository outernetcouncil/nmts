version: v2

deps:
  - buf.build/googleapis/googleapis

modules:
  - path: .
    name: buf.build/brumhard-testing/nmts

lint:
  use:
    - MINIMAL
  except:
    - PACKAGE_DIRECTORY_MATCH

breaking:
  use:
    - FILE
